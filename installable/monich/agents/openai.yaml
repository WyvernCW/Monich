interface:
  display_name: "Monich"
  short_description: "Premium scroll-driven showcase websites"
  brand_color: "#704421"
  default_prompt: "Use $monich to build a premium scroll-driven product showcase. Scan assets/templates including web-template, learn and adapt the references, write fresh code, use responsive motion, and copy no brand assets."

policy:
  allow_implicit_invocation: true
